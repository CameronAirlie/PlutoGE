#pragma once

namespace PlutoGE::render
{
    struct RenderGraphConfig
    {
    };

    struct RenderPass
    {
    };

    class RenderGraph
    {
    public:
        RenderGraph() = default;
        ~RenderGraph() = default;
    };
}